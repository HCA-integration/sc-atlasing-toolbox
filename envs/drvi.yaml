name: drvi
channels:
  - conda-forge
  # - pytorch
  # - nvidia
dependencies:
  - python=3.11
  - anndata=0.10
  - scanpy=1.10
  - zarr=2
  - scipy<1.15
  - hdf5=1.14.2
  - dask
  - sparse
  - pip
  - pip:
    - torch
    - torchvision
    - torchaudio
    - drvi-py
