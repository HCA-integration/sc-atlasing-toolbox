output_dir: test/out
images: test/images

resources:
  cpu:
    partition: cpu_p
    qos: icb
    gpu: 0
  gpu:
    partition: gpu_p
    qos: gpu
    gpu: 1

defaults:
  integration:
    scvi:
      n_hidden: 128
      dispersion:
        - gene
        - gene-batch
        - gene-label
        - gene-cell
      max_epochs: 10
      use_observed_lib_size: true
    scanvi:
      n_hidden: 128
      dispersion: gene
      max_epochs: 10
      use_observed_lib_size: true
    combat:
    scanorama:
    unintegrated:

DATASETS:
  test:
    label: bulk_labels
    batch: batch
    lineage: phase
    adata_file: ../../data/pbmc68k.h5ad
    integration:
        combat:
        scvi:
          max_epochs: 10
  test2:
    label: bulk_labels
    batch: batch
    lineage: phase
    adata_file: ../../data/pbmc68k.h5ad
    integration:
      combat: