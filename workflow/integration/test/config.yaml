output_dir: test/out

resources:
  cpu:
    partition: cpu_p
    qos: icb
  gpu:
    partition: gpu_p
    qos: gpu

defaults:
  integration:
    - scvi
    - scanvi
    - combat
    - scanorama
    - unintegrated

DATASETS:
  test:
    label: bulk_labels
    batch: batch
    adata_file: ../../data/pbmc68k.h5ad