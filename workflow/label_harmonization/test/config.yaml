output_dir: test/out
images: test/images

resources:
  cpu:
    partition: cpu_p
    qos: icb
    gpu: 0
  gpu:
    partition: gpu_p
    qos: gpu
    gpu: 1


# defaults:
#   label_harmonization:
#     celltypist:
#       use_rep: 'X_pca'
#       use_pct: true

DATASETS:
  blood_relabel:
    input:
      label_harmonization: test/input/load_data/filtered/SchulteSchrepping2020.zarr
    label_harmonization:
      mapping:
        file:  test/input/mapping.tsv
        order:
          - cell_type
          - harmonized_label
          - lineage
  test_celltypist:
    input:
      label_harmonization: test/input/preprocessing/test/neighbors.h5ad
    label_harmonization:
      dataset_key: phase
      author_label_key: bulk_labels
      celltypist:
        use_rep: 'X_pca'
        use_pct: true