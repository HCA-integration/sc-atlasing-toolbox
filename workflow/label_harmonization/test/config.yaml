output_dir: test/out
images: test/images

resources:
  cpu:
    partition: cpu
    qos: normal
    gpu: 0
  gpu:
    partition: gpu
    qos: normal
    gpu: 1


# defaults:
#   label_harmonization:
#     celltypist:
#       use_rep: 'X_pca'
#       use_pct: true

DATASETS:
  test_celltypist:
    input:
      label_harmonization:
        pbmc: test/input/preprocessing/all/pbmc/preprocessed.zarr
    label_harmonization:
      plot_colors:
        - phase
        - bulk_labels
      dataset_key: phase
      author_label_key: bulk_labels
      organ: blood
      celltypist:
        use_rep: X_pca
        use_pct: false
  test_celltypist_pct:
    input:
      label_harmonization:
        pbmc: test/input/preprocessing/all/pbmc/preprocessed.zarr
    label_harmonization:
      plot_colors: bulk_labels
      dataset_key: phase
      author_label_key: bulk_labels
      organ: blood
      celltypist:
        use_rep: X_pca
        use_pct: true

ORGANS:
  blood:
    marker_genes:
      - ITGB2
      - HES4
      - CD14
      - SSU72
      - S100B