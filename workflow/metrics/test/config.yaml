output_dir: test/out

DATASETS:
  test:
    label: bulk_labels
    batch: batch
    adata_file: ../../data/pbmc68k.h5ad
    metrics:
      - nmi
      - ari
      - asw_label
      - asw_batch
      - cell_cycle
      - clisi
      - ilisi
      - graph_connectivity
      - isolated_label_asw
      - isolated_label_f1
      - pcr
  test2:
    label: bulk_labels
    batch: batch
    adata_file: ../../data/pbmc68k.h5ad
    metrics:
      - nmi