output_dir: test/out

DATASETS:
  test:
    label: bulk_labels
    batch: batch
    adata_file: ../../data/pbmc68k.h5ad
    metrics:
      #- asw_batch
      #- graph_connectivity
      #- ilisi
      #- pcr
      #- ari
      #- asw_label
      #- cell_cycle
      #- clisi
      #- isolated_label_asw
      #- isolated_label_f1
      - nmi
      - ari
  test2:
    label: bulk_labels
    batch: batch
    adata_file: ../../data/pbmc68k.h5ad
    metrics:
      - nmi